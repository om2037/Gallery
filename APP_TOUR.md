# تور اپلیکیشن Cloud Gallery

به Cloud Gallery خوش آمدید! این اپلیکیشن به شما اجازه می‌دهد تا از حساب تلگرام خود به عنوان یک فضای ابری شخصی برای ذخیره و مشاهده عکس‌ها، ویدیوها و بکاپ پیامک‌های خود استفاده کنید. این راهنما شما را با تمام قابلیت‌های برنامه آشنا می‌کند.

---

### فصل ۱: اولین اجرا و ورود به سیستم

برای استفاده از اپلیکیشن، ابتدا باید آن را به حساب تلگرام خود متصل کنید.

**۱.۱. صفحه ورود (`LoginScreen`)**

هنگامی که برای اولین بار برنامه را باز می‌کنید، صفحه ورود به شما نمایش داده می‌شود. در این صفحه باید اطلاعات زیر را با دقت وارد کنید:

*   **API ID و API Hash:** این اطلاعات را باید از پلتفرم تلگرام (my.telegram.org) دریافت کنید. این دو مقدار، کلیدهای دسترسی اختصاصی شما برای استفاده از API تلگرام هستند.
*   **Phone Number:** شماره تلفن مرتبط با حساب تلگرام خود را وارد نمایید.
*   **Channel ID:** شناسه عددی کانال تلگرامی که به عنوان فضای ذخیره‌سازی از آن استفاده می‌کنید را وارد کنید. (مثلاً: `-100123456789`)

پس از تکمیل تمام فیلدها، دکمه **Login** را فشار دهید.

**۱.۲. صفحه تایید هویت (`OtpScreen`)**

پس از فشردن دکمه Login، تلگرام یک کد تایید یکبار مصرف (OTP) به حساب شما ارسال خواهد کرد.

*   کد دریافت شده را در فیلد **OTP** وارد کنید.
*   دکمه **Verify** را برای تکمیل فرآیند ورود فشار دهید.

در صورت موفقیت‌آمیز بودن، شما به صفحه اصلی اپلیکیشن هدایت خواهید شد. از این پس، تا زمانی که از حساب خود خارج نشوید، برنامه شما را به صورت خودکار وارد خواهد کرد.

---

### فصل ۲: صفحه اصلی - مرکز فرماندهی شما

صفحه اصلی (`MainScreen`) نقطه شروع شما برای دسترسی به تمام محتوای برنامه است.

*   **نوار ابزار بالا (`TopAppBar`):**
    *   **عنوان برنامه:** "Cloud Gallery".
    *   **دکمه همگام‌سازی (Sync):** آیکون تقویم در گوشه بالا سمت راست به شما اجازه می‌دهد تا یک **همگام‌سازی هوشمند** انجام دهید. با کلیک بر روی آن، یک پنجره انتخاب تاریخ باز می‌شود. شما می‌توانید یک تاریخ را به عنوان **نقطه شروع همگام‌سازی** انتخاب کنید. با این کار، برنامه فقط فایل‌های جدیدتر از آن تاریخ را از کانال تلگرام دانلود می‌کند. این قابلیت برای صرفه‌جویی در زمان و مصرف اینترنت بسیار مفید است.

*   **نوار ناوبری پایین (`BottomNavigationBar`):**
    این نوار دارای دو تب اصلی برای جابجایی بین بخش‌های مختلف است:
    *   **Gallery:** برای مشاهده عکس‌ها و ویدیوهای شما.
    *   **Sms:** برای دسترسی به لیست بکاپ‌های پیامک.

---

### فصل ۳: کاوش در گالری رسانه‌ها

این بخش، تجربه‌ای مشابه Google Photos را برای مرور عکس‌ها و ویدیوهای شما فراهم می‌کند.

**۳.۱. صفحه گالری (`GalleryScreen`)**

*   **نمایش هوشمند:** تمام رسانه‌ها به صورت خودکار بر اساس تاریخ ثبتشان گروه‌بندی شده و به ترتیب از جدیدترین به قدیمی‌ترین نمایش داده می‌شوند.
*   **مرور آسان:** شما می‌توانید به راحتی در لیست اسکرول کرده و به تاریخ مورد نظر خود برسید.
*   **دسترسی به جزئیات:** با کلیک (لمس) بر روی هر عکس یا ویدیو، به صفحه نمایش جزئیات آن منتقل خواهید شد.

**۳.۲. صفحه جزئیات رسانه (`MediaDetailScreen`)**

*   **نمایش تمام‌صفحه:** رسانه انتخابی شما به صورت تمام‌صفحه نمایش داده می‌شود.
*   **قابلیت زوم:** شما می‌توانید با دو انگشت خود (ژست pinch-to-zoom) بر روی تصاویر زوم کرده و جزئیات آن را مشاهده کنید.
*   **دکمه اطلاعات (`Info`):** با لمس آیکون اطلاعات در پایین صفحه، یک منوی کشویی باز می‌شود که شامل موارد زیر است:
    *   **اطلاعات فایل:** مدل دستگاه و تاریخ دقیق ثبت فایل.
    *   **دکمه `Download`:** برای ذخیره فایل بر روی حافظه داخلی دستگاه شما.
    *   **دکمه `Delete`:** برای حذف فایل از اپلیکیشن.

---

### فصل ۴: دسترسی به بکاپ‌های SMS

این بخش به شما اجازه می‌دهد تا بکاپ‌های پیامک خود را که در قالب فایل‌های HTML در کانال تلگرام ذخیره کرده‌اید، مشاهده کنید.

**۴.۱. صفحه لیست پیامک‌ها (`SmsScreen`)**

*   **لیست بکاپ‌ها:** در این صفحه، لیستی از تمام فایل‌های بکاپ SMS به همراه تاریخ و نام دستگاه مربوط به هر بکاپ نمایش داده می‌شود.
*   **دسترسی به محتوا:** با کلیک بر روی هر آیتم در لیست، به صفحه نمایش محتوای آن بکاپ هدایت می‌شوید.

**۴.۲. صفحه نمایش وب (`WebViewScreen`)**

*   **نمایش محتوا:** این صفحه محتوای فایل HTML بکاپ را به صورت کامل و با همان ساختاری که ذخیره شده است، نمایش می‌دهد. شما می‌توانید در میان پیامک‌های خود اسکرول کرده و آن‌ها را مطالعه کنید.

---

امیدواریم این تور متنی به شما در استفاده بهتر از اپلیکیشن Cloud Gallery کمک کند.
